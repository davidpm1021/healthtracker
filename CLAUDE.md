# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This is a HealthTracker project that appears to be in its initial setup phase. The repository currently contains empty starter files and is ready for development.

## Project Structure

The project has a minimal structure:
- `index.js` - Main entry point (currently empty)
- `README.md` - Project documentation (currently empty)
- `ProjectPlan.md` - Project planning document (currently empty)

## Development Commands

Since this is a new project without package.json or other configuration files, there are currently no build, lint, or test commands configured. When developing this project:

1. **JavaScript Development**: The project appears to be a JavaScript application based on the presence of `index.js`
2. **Package Management**: Consider initializing with `npm init` or `yarn init` to set up package.json
3. **Dependencies**: No dependencies are currently installed

## Architecture Notes

As this is a greenfield project, the architecture is yet to be defined. Based on the project name "HealthTracker", this will likely be a health monitoring or tracking application.

## Getting Started

To begin development:
1. Initialize the project with a package manager
2. Set up the basic project structure
3. Configure linting and testing frameworks
4. Implement the core functionality in `index.js`

## Version Control

The project is initialized with Git and is on the master branch with an initial commit.